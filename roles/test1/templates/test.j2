- class_name: org.apache.cassandra.locator.SimpleSeedProvider
  parameters:
    # seeds is actually a comma-delimited list of addresses.
    # Ex: "<ip1>,<ip2>,<ip3>"
    - seeds: "{{ groups['cassandra_node'] | map('extract', hostvars, ['ip']) | join(',') }}"
